var array = ["@keyframes", "animation", "animation-name",
    "animation-duration", "animation-timing-function", "animation-delay",
    "animation-iteration-count", "overflow-x", "overflow-y", "color-opacity"
];

var rand = array[Math.floor(Math.random() * array.length)];

function setNew()
{

}